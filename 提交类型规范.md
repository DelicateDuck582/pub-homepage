# 提交类型规范（工作日志自动分类用）

> 给 AI 代理 / 协作者看：**每次 push 之前，commit message 必须写明类型**。
> 工作日志页（worklog）会按这个类型给每一次提交打标签、画图；写不清楚就会被兜底成灰色的「其它」。

## 一、commit message 长什么样

```
<type>(<scope>): <描述>
```

- `type` 必须是小写英文，只能取下面 13 个之一；
- `scope` 可选，写模块名或目录名，如 `login`、`worker`、`cos-exchange`、`browse`；
- 冒号用半角 `:`，后面跟一个空格再写描述；描述用中文、英文都行，但要说清"做了什么"；
- 一次提交只做一件事，混在一起就拆成多条；
- 正确示例：
  - `feat(worker): /temp 在线查看支持 Range 分段下载`
  - `fix(login): 修复验证码 5 分钟后仍可通过的问题`
  - `refactor(cos-exchange): 抽出签名校验函数，去掉重复分支`
  - `docs: 补充部署步骤与常见报错说明`

## 二、13 个类型怎么判

| type | 中文 | 判定标准（满足其一） | 典型改动 | message 示例 |
|---|---|---|---|---|
| `feat` | 新功能 | 新增用户能感知的能力、接口、页面、配置项 | 新组件 / 新接口 / 新开关 | `feat: 支持挂载阿里云盘浏览` |
| `fix` | 修复 | 修正错误行为、崩溃、安全漏洞、显示错乱、兼容问题 | bug 修复、安全加固 | `fix(xss): 入库前对 HTML 签名做白名单清洗` |
| `refactor` | 重构 | 不改行为的结构调整：抽函数、拆文件、重命名、删废弃代码、把别处的实现同步过来 | 结构变化、代码搬移 | `refactor(worker): 与 browse-alist 同步签名逻辑` |
| `perf` | 性能 | 明确为了更快 / 更小 / 更省：缓存、懒加载、去重、减体积 | 缓存、索引、虚拟滚动 | `perf(list): 列表改为虚拟滚动，首屏更快` |
| `docs` | 文档 | 只动文档、注释、说明文案（不改代码行为） | `.md` / 注释 | `docs: 补充 /temp 接口说明` |
| `style` | 样式 | 只动视觉与排版：CSS、主题、间距、字号、图标；或纯格式化 | `.css` / `.scss`、UI 微调 | `style: 统一卡片圆角与间距` |
| `test` | 测试 | 只动测试代码或测试数据 | `*.test.*` / `*.spec.*` | `test: 补 /temp 的边界用例` |
| `build` | 构建 | 构建工具与产物配置：打包入口、`tsconfig`、锁文件 | `vite.config` / `webpack` | `build: 打包入口改为 ESM` |
| `ci` | CI | 流水线、工作流、部署脚本 | `.github/workflows/*` | `ci: 增加 PR 自动检查` |
| `chore` | 杂项 | 与功能无关的琐事：依赖升级、版本号、忽略文件、临时诊断输出 | `package.json` / `.gitignore` | `chore: 升级 vite 到 5.4` |
| `merge` | 合并 | 合并分支 / 上游 / PR 产生的 merge commit | merge commit | `merge: 合并上游 v3.2.0` |
| `revert` | 回滚 | 撤销之前的提交 | revert commit | `revert: 回滚 3f2a1c 的缓存改动` |
| `other` | 其它 | 实在不好归类（**尽量别用**，页面上会显示成灰色「其它」） | — | — |

### 冲突时按这个顺序判断

1. 修错误、崩溃、漏洞、兼容 → `fix`（**不要因为顺手重构就写成 refactor**）；
2. 新增能力 → `feat`；把已有能力同步 / 搬过来 → `refactor`；
3. 只动文档 → `docs`；只动样式 → `style`；只动测试 → `test`；
4. 依赖 / 版本 / 忽略文件 / 诊断日志 → `chore`；构建工具配置 → `build`；流水线 → `ci`；
5. 一次提交横跨多类内容 → 拆开提交，按"主要意图"选 type。

## 三、给 AI 代理的执行要求（必读）

1. **写 message 时第一段必须是 `<type>(<scope>): 描述`**，type 只能取上表 13 个之一（小写）；
   不要自造前缀——`diagnose:`、`security:`、`sync:`、`update:`、`improve:` 这类会被兜底或归一，属于不合规写法。
2. 提交前自检：把 message 拿去过一遍上一节的判定顺序，确认 type 与改动内容一致。
3. **push 之前**检查这一批所有 commit：`git log --pretty=%s origin/HEAD..HEAD`；
   遇到没前缀或前缀不合规的，用 `git commit --amend`（只改最后一条）或 `git rebase -i` 改好再推。
   改 message 是安全操作、不动代码；但**已经推上公共分支的不要强推**——那种情况在下一次提交里写规范即可。
4. 合并上游产生的 merge commit，message 以 `merge:` 开头并写明来源（`merge: 合并上游 v3.2.0`）。
5. 不要为了整齐改写别人的提交；只规范自己这次要推的提交。

## 四、分类器怎么判（可对照自查）

工作日志的采集脚本 `_collect.py::classify()` 按顺序做三件事：

1. **规范前缀**：`feat:` / `fix(scope):` / `docs：` / `feat xxx` → 直接采用。
   别名会被归一，常见等价写法：`feature|impl|add → feat`、`bugfix|hotfix|security → fix`、
   `format|lint → style`、`deps|release|bump → chore`、`sync|port|clean → refactor`、`diagnose|debug|log → chore`。
2. **没有前缀就按措辞猜**（顺序即优先级）：回滚 → `revert`；修复 / 兼容 / 报错 / 失效 → `fix`；
   新增 / 支持 / 实现 / 接入 → `feat`；重构 / 优化 / 调整 / 移除 / 清理 → `refactor`；
   性能 / 缓存 / 懒加载 / 体积 → `perf`；文档 / 注释 → `docs`；测试 / 用例 → `test`；
   样式 / 布局 / 图标 → `style`；打包 / 构建配置 → `build`；流水线 / 工作流 → `ci`；依赖 / 版本 / 发布 → `chore`。
3. **还猜不到就看改动文件**：只动 `.md` → `docs`；只动测试文件 → `test`；只动配置 / 锁文件 → `chore`；
   其余落到 `other`（日志里显示灰色「其它」）。

> 结论：**写规范前缀是唯一 100% 可控的做法**，关键词与文件兜底只是保险。

## 五、现有提交的修订对照

按上面的规则重新判定后，原先落进「其它」的几条都属于前缀不合规，改法如下：

| 原 message（节选） | 规范写法 | 判定结果 |
|---|---|---|
| `diagnose: /browse 列表签名失败时返回两侧 StringToSign 对照` | `chore(diagnose): 列表签名失败时输出两侧 StringToSign 对照` | chore（临时诊断输出） |
| `security: 在 main 分支应用纯安全加固（基于上游 v3.2.0）` | `fix(security): 应用上游 v3.2.0 的安全加固` | fix |
| `security: HTML 签名入库前白名单清洗（防存储型 XSS）` | `fix(xss): HTML 签名入库前做白名单清洗` | fix |
| `sync(cos-exchange): 与 browse-alist 同步 worker（…）` | `refactor(cos-exchange): 同步 browse-alist 的 worker（…）` | refactor |

## 六、可选：让 git 自己拦一下

在仓库里放一个 `commit-msg` 钩子（`.git/hooks/commit-msg`，记得 `chmod +x`），不合规的 message 直接拦住：

```sh
#!/bin/sh
# 只允许 <type>(<scope>): 描述 这种写法；merge / revert commit 豁免
msg=$(head -n1 "$1")
case "$msg" in
  Merge*|merge:*|Revert*|revert:*) exit 0 ;;
esac
if echo "$msg" | grep -Eq '^(feat|fix|refactor|perf|docs|style|test|build|ci|chore)(\(.+\))?: .+'; then
  exit 0
fi
echo "提交信息不规范：请写成 <type>(<scope>): 描述" >&2
echo "可用 type：feat fix refactor perf docs style test build ci chore merge revert" >&2
echo "详见 提交类型规范.md" >&2
exit 1
```

---

**相关文件**：分类实现 `_collect.py`（`KIND_ALIAS` / `KIND_WORDS` / `classify`）；页面生成 `_gen_worklog.py`（`KIND_LABEL` / `KIND_ORDER`）；每日自动更新 `_daily_update.ps1`。
